import RouteLoadingState from "@/app/components/loading/RouteLoadingState";

export default function CommercialColorationLoading() {
	return (
		<RouteLoadingState
			title="Coloración"
			subtitle="Cargando cartas de color y referencias para asesoramiento técnico."
			variant="grid"
		/>
	);
}
